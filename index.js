//infer types(implicit types)
var username = "afeeda";
username = 0;
var userDeta = {
    name: "afeda",
    age: 28,
};
var currentstatus = "pending ";
//from api
var response = "pending";
if (response === "pending") {
    currentstatus = "pending ";
}
